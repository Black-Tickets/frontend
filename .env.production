# Production environment for Kubernetes/Docker deployment
# For Kubernetes: leave empty to use relative paths (routed via Ingress)
# For local Docker with nginx: set to http://localhost
VITE_API_BASE_URL=
